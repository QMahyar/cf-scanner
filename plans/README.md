# Implementation Plans

Generated by the improve skill on 2026-08-16 from the 10-agent product review
(2026-08-13) + user decisions. Base commit: `cd4e3a5` (engine + WARP hardening
R1/R2 already landed on main). Each plan was executed on its own
`review/<slug>` branch from that base, then merged to main after review
(merge order 001 → 002 → 003 → 004 → 005; 003/005 conflict on
`docs/release-process.md` resolved in the 005 merge).

## Execution order & status

| Plan | Title                        | Priority | Effort | Depends on | Status |
|------|------------------------------|----------|--------|------------|--------|
| 001  | Server + CLI hardening       | P1       | L      | —          | DONE   |
| 002  | Frontend fixes               | P2       | M      | 001 (API)  | DONE   |
| 003  | Docs + ADRs + CHANGELOG      | P2       | M      | —          | DONE   |
| 004  | Test hardening               | P2       | M      | —          | DONE   |
| 005  | CI/release hardening         | P1       | M      | —          | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (reason)

## Dependency notes

- 002 depends on 001 only for the `/api/warp/register` `overwrite` field —
  the frontend change works independently; merge order 001 then 002.
- 004 touches `src/verify.rs` + `Cargo.toml` (dev-dep). 001 touches
  `src/server.rs`/`src/main.rs`/`src/cli_wizard.rs`. No file overlap with any
  other plan; branches can merge in any order.
- Version bump to 0.4.0 + CHANGELOG release section: done by the integrator
  AFTER all five branches merge (not in plan 003).

## Findings considered and rejected

- Build.rs graceful GeoIP degrade: rejected by user decision — build keeps
  hard-failing; docs get rewritten to state that behavior (plan 003 item 1).
- API auth token: rejected by user decision — minimal hardening only
  (plan 001 items S3/S4/S6).
- Domain-layer contract refactor: rejected by user decision — shared
  `api::types` stays the contract; recorded in ADR-011 (plan 003).
- `--open` browser flag: deferred — would need a new dependency (webbrowser);
  not approved.