# Release process — versioning and publishing

The single source of truth for how CF-Scanner is versioned, tagged, and
published. Follow this for every release. CI files: `.github/workflows/checks.yml`
(PR/main gates) and `.github/workflows/release.yml` (generated by cargo-dist 0.32 —
do not hand-edit; re-run `dist init`).

## Versioning control

- **Semantic versioning.** MAJOR = breaking change (spec: API contract or
  behavior changes), MINOR = new backward-compatible functionality, PATCH =
  backward-compatible fix. API contract changes (`src/api/`) are
  MINOR-or-MAJOR by definition — ask first.
- **The git tag is the source of truth.** `v<MAJOR>.<MINOR>.<PATCH>` (e.g.
  `v0.1.0`). cargo-dist derives the announced version from the tag; the
  artifact, the tag, and the changelog must never disagree. `Cargo.toml`
  `version` must match the tag you intend to release.
- **Changelog entries are written with the change**, not reconstructed at
  release time (`CHANGELOG.md`, Keep-a-Changelog format, newest on top).
  At release time only the `## [Unreleased]` header becomes
  `## [x.y.z] - YYYY-MM-DD`.

## Pre-flight checklist (before tagging)

1. `cargo test` — all green.
2. `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check`.
3. `cargo audit` — 0 vulnerabilities.
4. `git status` clean; the release commit contains the changelog cut.
5. Optional but recommended: local `dist build --artifacts=local` smoke test
   (see `docs/development.md`; restore the placeholder afterwards).
6. No secrets in the diff (`git diff | grep -i secret`).

## Publishing pipeline

```
changelog cut (release commit on main)
        │
        ▼
git tag -a vX.Y.Z -m "cf-scanner vX.Y.Z"
git push origin vX.Y.Z          # the only trigger — never "publish" manually
        │
        ▼
GitHub Actions "Release" workflow (release.yml)
  plan        → resolves the 3-target matrix + manifest
  build-local → per target: dist build (bundles checksum-verified xray),
                archives + .sha256 + MSI (windows runner ships WiX)
  build-global→ aggregate sha256.sum, source tarball
  host        → uploads artifacts to the GitHub Release (vX.Y.Z)
  announce    → release notes from the changelog
```

The push of `main` also triggers the `Checks` workflow (test + clippy + fmt).
Tag pushes and PRs additionally run the Release workflow in "plan" mode.

## Post-publish verification

1. `gh run watch <run-id>` to completion.
2. `gh release view vX.Y.Z` — 3 platform archives + installers + sha256 sums
   present, `isPrerelease: false`.
3. Spot-check: download one archive, compare its SHA-256 against the
   published `.sha256`, and confirm the real xray binary (non-zero size) is
   inside next to `cf-scanner`.

## Fixing a broken release

If CI fails or artifacts are wrong (e.g. the v0.1.0 xray asset-name failure):

1. Fix on `main` with the normal gates; let `Checks` pass.
2. Move the tag to the fixed commit and re-push it:
   ```
   git tag -f vX.Y.Z            # tag now points at the fix commit
   git push origin :vX.Y.Z      # delete remote tag
   git push origin vX.Y.Z       # re-push → fresh Release run
   ```
3. Verify again per "Post-publish verification". The failed run stays in
   history as-is; it publishes nothing (the `host` job requires all builds
   green), so no orphan release can exist.

Only a release that has NOT been announced can be moved like this. Once a
release exists, never rewrite it — cut a new PATCH.

## Known gotchas

- **XTLS asset naming drifts** (arm64 `-v8a` suffix, `macos` not `darwin`).
  If a build job dies downloading xray, check `build.rs::xray_asset` against
  the real asset list before touching anything else.
- **gh token needs `workflow` scope** to push repos that contain workflow
  files: `gh auth refresh -h github.com -s workflow`.
- **The 0-byte `data/bundled/*` placeholders are git-tracked** — release
  builds overwrite one of them locally during `dist build` and delete the
  other target's placeholder (later builds recreate it); always restore both
  before committing (`git restore data/bundled/xray data/bundled/xray.exe`).
  CI is unaffected (per-job checkouts).
- **Windows binaries are unsigned** → SmartScreen warning. Accepted.
- **WiX is CI-only** — MSI builds fail on dev boxes without WiX. Expected.

## Rollback plan

There is no server deployment to roll back: users install self-contained
artifacts from a GitHub Release. "Rollback" = releasing a PATCH that fixes a
broken prior release, or telling users to install the previous release
(previous tags remain downloadable forever). Never delete or edit a published
release.
